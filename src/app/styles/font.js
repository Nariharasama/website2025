import { Space_Grotesk } from "next/font/google";


const SpaceLight = Space_Grotesk({
    subsets: ['latin'],
    weights: 300,

})

const SpaceReg = Space_Grotesk({
    subsets: ['latin'],
    weights: 400,

})

const SpaceMed = Space_Grotesk({
    subsets: ['latin'],
    weights: 500,

})

const SpaceBold = Space_Grotesk({
    subsets: ['latin'],
    weights: 700,

})

export {SpaceLight, SpaceReg, SpaceMed, SpaceBold}

